decl
	int n,temp;
enddecl

int main()
{
	begin
		temp=exposcall("Fork");
		if(temp==0)then
			//child process
			n=exposcall("Exec","odd.xsm");
		else
			n=exposcall("Exec","even.xsm");
		endif;
		return 0;
	end
}